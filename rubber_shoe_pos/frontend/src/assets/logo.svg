import { ReactComponent as Logo } from "../assets/logo.svg";

function TopBar({ user = "Admin User", role = "OWNER" }) {
  return (
    <header className="flex justify-between items-center p-4 bg-white shadow-md">
      <div className="flex items-center space-x-3">
        <Logo className="w-12 h-12" />
        <span className="text-xl font-semibold">Timeless Shoes <strong>POS System</strong></span>
      </div>
      <div className="flex items-center space-x-3">
        <div className="text-right">
          <div>{user}</div>
          <div className="text-sm bg-blue-600 text-white px-2 py-0.5 rounded">{role}</div>
        </div>
        <img className="w-8 h-8 rounded-full" src="https://i.pravatar.cc/32" alt="avatar"/>
      </div>
    </header>
  );
}
